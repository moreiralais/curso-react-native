import { StyleSheet } from "react-native";

const estilo = StyleSheet.create({
    naMesmaLinha: {
        flexDirection: "row",
        alignItems:"center"
    },
    imgSend:{
        width:30,
        height:30,
        marginRight:10
    },
    comentario:{
        flex:1
    }
})

export default estilo;